{
  "name": "my-fcc-personal-portfolio-webpage",
  "version": "1.0.0",
  "description": "My freeCodeCamp Personal Portfolio Webpage project",
  "main": "index.html",
  "scripts": {
    "build": "rm dist/ -rf && mkdir dist && cp public/* dist/ -r && yarn node-sass src/ -o dist/ --output-style compressed",
    "deploy-gh-pages": "gh-pages -d dist",
    "sass-watch": "node-sass src/ -o dist/ -w"
  },
  "repository": "https://github.com/WofWca/my-fcc-personal-portfolio-webpage.git",
  "author": "WofWca",
  "license": "MIT",
  "dependencies": {
    "node-sass": "^4.11.0"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
